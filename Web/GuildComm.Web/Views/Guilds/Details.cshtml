@using GuildComm.Web.ViewModels.Guild
@model GuildDetailsViewModel

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="text-center">Guild Details</h1>
<hr class="hr-2">
<div class="mx-auto">
    <div class="d-flex">
        <h3>Name: @Model.Name</h3>
    </div>
    <hr class="hr-2">
    <div class="d-flex">
        <h4>Realm: @Model.RealmName @Model.RealmRegion</h4>
    </div>
</div>
<h1 class="text-center">Members</h1>
<hr class="hr-2">
<table class="table table-hover">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Name</th>
            <th scope="col">Class</th>
            <th scope="col">Level</th>
            <th scope="col">Rank</th>
            <th scope="col">Member Since</th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Model.Members.Count; i++)
        {
            <tr>
                <th scope="row">@(i + 1)</th>
                <td>@Model.Members[i].Character.Name</td>
                <td>@Model.Members[i].Character.Class</td>
                <td>@Model.Members[i].Character.Level</td>
                <td>@Model.Members[i].Rank</td>
                <td>@Model.Members[i].MemberSince</td>
                @*<td><a class="btn btn-outline-danger btn-sm text-danger" asp-controller="Characters" asp-action="Remove" asp-route-id="@Model.Members[i].Id">Remove</a></td>
                    <td><a class="btn btn-outline-primary btn-sm text-primary" asp-controller="Characters" asp-action="Details" asp-route-id="@Model.Members[i].Id">Details</a></td>*@
            </tr>
        }
    </tbody>
</table>

@if (Model.UserCharacters.Any())
{
    <h1 class="text-center">Your Characters</h1>
    <hr class="hr-2">
    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Name</th>
                <th scope="col">Class</th>
                <th scope="col">Level</th>
                <th scope="col">Rank</th>
                <th scope="col">Member Since</th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.UserCharacters.Count; i++)
            {
                <tr>
                    <th scope="row">@(i + 1)</th>
                    <td>@Model.UserCharacters[i].Character.Name</td>
                    <td>@Model.UserCharacters[i].Character.Class</td>
                    <td>@Model.UserCharacters[i].Character.Level</td>
                    <td>@Model.UserCharacters[i].Rank</td>
                    <td>@Model.UserCharacters[i].MemberSince</td>
                    <td><a class="btn btn-outline-danger btn-sm text-danger" asp-controller="Guilds" asp-action="RemoveMember" asp-route-id="@Model.Members[i].Id">Leave</a></td>
                </tr>
            }
        </tbody>
    </table>
}
